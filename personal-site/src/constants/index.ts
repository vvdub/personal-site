export const CODE_FRAGMENTS = [
  'const build = () => ship();',
  'while (true) { create(); }',
  'git push origin main',
  'npm run deploy',
  '#!/usr/bin/env node',
  'export default future;',
  'rm -rf node_modules && pnpm i',
  'docker compose up -d',
  'SELECT * FROM ideas;',
  'curl -X POST /api/v1/launch',
  '> compiling...',
  '200 OK',
  'ssh root@production',
  'tail -f /var/log/output',
  'echo "building software"',
  '{ "status": "live" }',
]

export const GLITCH_CHARS = '01!@#$_-=|;:<>/~[]{}()&%^*+\\?.'

// WarpCanvas config
export const STAR_COUNT = 400
export const MAX_DEPTH = 1500
export const CODE_PARTICLE_COUNT = 40

// Intro timing (ms)
export const TOTAL_WARP_MS = 4800
export const FLASH_AT_MS = 4800
export const COMPLETE_AT_MS = 5400

// BPM
export const BPM_INITIAL = 128
export const BPM_MAX = 180
